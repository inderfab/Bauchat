PDF-Chat Application for Swiss Architects
A Streamlit aplication lets user upload PDF Files or select collections of PDFs (S3 Storage).
The textcontent is embedded in an Vectorspace (OpenAi) and can be queried with vector similiarity search (FAISS). 
The k closest searches are then translated to a answer (ChatGPT) and the source PDF pages is displayed as image.
